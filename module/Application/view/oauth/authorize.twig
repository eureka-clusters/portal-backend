<div class="container">
    <h1>{{ translate("txt-authorize") }}</h1>

    <form method="post">
        <label>Do You Authorize</label><br/>
        <small class="text-muted">{{ clientId }}</small>
        <br>
        <br>
        <button class="btn btn-primary" type="submit" name="authorized" value="yes">{{ translate("txt-yes") }}</button>
        <button class="btn btn-secondary" type="submit" name="authorized" value="no">{{ translate("txt-no") }}</button>
    </form>
</div>
