{% do headTitle().append(translate("txt-admin") ) %}
{% do headTitle().append(translate("txt-new-role")) %}

<h1>{{ translate("txt-new-role-title") }}</h1>
<p>{{ translate("txt-new-role-explanation") }}</p>

{% do form.prepare() %}
{{ form().openTag(form)|raw }}

{% set roleForm = form.get('admin_entity_role') %}

{{ formelement(roleForm.get('id')) }}
{{ lbs5formelement(roleForm.get('description')) }}

<div class="row mb-3">
    <div class="offset-sm-3 col-sm-9">
        {{ formelement(form.get('csrf')) }}
        {{ lbs5formelement(form.get('submit')) }}
        {{ lbs5formelement(form.get('cancel')) }}
    </div>
</div>

{{ form().closeTag()|raw }}
