{% do headTitle().append(translate("txt-admin") ) %}
{% do headTitle().append(translate("txt-edit-oauth2-service")) %}

<h1>{{ translate("txt-edit-oauth2-service-title") }}</h1>
<p>{{ translate("txt-edit-oauth2-service-explanation") }}</p>

{% include 'admin/partial/form/oauth2/service' %}