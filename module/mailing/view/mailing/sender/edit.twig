{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-sender-%s")|format(sender)) %}

<h1>{{ translate("txt-edit-sender-%s")|format(sender) }}</h1>

{% include 'mailing/partial/form/sender' %}

