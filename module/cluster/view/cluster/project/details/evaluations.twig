{% do headTitle().append(translate("txt-admin") ) %}
{% do headTitle().append(project.name) %}
{% import 'cluster/partial/project/details-formatters' as details %}

<h1>{{ project.name }}</h1>

<p>Evaluations</p>

{% include 'cluster/partial/project/details-navigation' with {'navigation': navigation} %}

{% for group in groups|filter(group => group.rows) %}
    <h2>{{ group.title }}</h2>

    <table class="table table-striped table-sm table-hover">
        <thead>
        <tr>
            <th>{{ translate('txt-id') }}</th>
            <th>{{ translate('txt-country') }}</th>
            <th>{{ translate('txt-project-status') }}</th>
            <th>User</th>
            <th>{{ translate('txt-date-created') }}</th>
            <th>{{ translate('txt-description') }}</th>
        </tr>
        </thead>
        <tbody>
        {% for row in group.rows %}
            <tr>
                <td><small class="text-muted">{{ row.id }}</small></td>
                <td>{{ row.country }}</td>
                <td>{{ row.status }}</td>
                <td>{{ row.user }}</td>
                <td>{{ details.date(row.dateCreated, true) }}</td>
                <td>{{ details.text(row.description) }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% else %}
    {{ lbs5alert().info(translate("txt-no-evaluations-found"))|raw }}
{% endfor %}
